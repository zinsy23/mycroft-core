# Offline Mycroft Requirements
# Modified version of requirements.txt with dependency fixes for modern systems

# Core networking and communication
requests>=2.26.0,<3.0
gTTS>=2.2.2,<2.3.0
tornado>=6.1,<7.0
websocket-client>=1.2.1,<2.0
requests-futures==0.9.5

# Audio processing
PyAudio==0.2.11
SpeechRecognition==3.8.1
# pocketsphinx==0.1.0  # Optional: Only needed for pocketsphinx wake word engine
                       # Fails on x86_64 with GCC 13+ (C23 bool typedef issue)
                       # Works on ARM/Pi - provides "wake up" alternative wake word
                       # Not needed for primary functionality (Precise + FasterWhisper)

# System utilities
pyserial==3.0
psutil>=5.6.6
python-dateutil~=2.6.0
fasteners>=0.14.1
PyYAML>=5.4
pyxdg>=0.27

# Image processing
Pillow>=8.3.2

# Event handling - match mycroft-messagebus-client dependency
pyee==8.1.0

# Mycroft core components
# Pinned to known-good versions for stability
lingua-franca==0.4.3
msm==0.9.0
msk==0.4.0
# Use older messagebus client to avoid threading issues (will be patched)
mycroft-messagebus-client==0.9.6
adapt-parser>=0.3.0
padaos>=0.1.9
petact>=0.1.2
precise-runner==0.3.1

# Intent parsing (padatious) - critical for skills
# padatious>=0.4.8  # Installed separately due to fann2 dependency issues
# xxhash  # Required dependency for padatious, installed separately

# Wake word detection (Precise) - TensorFlow installed conditionally if needed
# precise-runner already listed above at line 42 with pinned version

# Optional components (may fail on some systems)
# fann2>=1.0.7  # padatious dependency, will be handled with dummy module

# OVOS packages - pinned to known-good versions for mycroft-core compatibility
# DO NOT upgrade - newer versions (2.x) are designed for OVOS-core, not mycroft-core
# These versions have been tested and confirmed working with mycroft-core
# Ordered: base dependencies first, then packages that depend on them
ovos-utils==0.8.4
ovos-config==0.0.12
ovos-bus-client==1.3.7
ovos-plugin-manager==0.9.0

# STT dependencies - pinned for stability
# ctranslate2 is the inference backend, faster-whisper uses it
ctranslate2==4.6.2
faster-whisper==1.2.1

# NOTE: CUDA runtime libraries (nvidia-cudnn-cu12, nvidia-cublas-cu12, nvidia-cuda-runtime-cu12)
# are installed separately by dev_setup.sh if user enables GPU acceleration for STT

# OVOS STT plugin (requires all of the above)
ovos-stt-plugin-fasterwhisper==0.2.0

# Wake word plugin for OpenWakeWord (optional - installed if selected during setup)
# ovos-ww-plugin-openwakeword==0.4.1
# openwakeword==0.6.0

# Dependencies for working skills only
# ifaddr>=0.1.0        # Was for mycroft-ip (removed)
# holidays>=0.10       # Was for mycroft-date-time (removed)  
# ovos-workshop>=0.0.5 # Was for OVOS skills (removed)
# pytz>=2020.1         # Was for mycroft-date-time (removed)
# timezonefinder>=8.0.0 # Was for mycroft-date-time (removed)
